function UnmountComponent(){
  return(
    <>
      <h1>Life Cycle UnMounting Phase</h1>
      <p>Unmounting refers to the lifecycle stage when a component is being removed from the DOM (Document Object Model) and is no longer rendered or accessible.</p>
    </>
  )
}

export default UnmountComponent;